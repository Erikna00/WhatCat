#######
Modules
#######



    ########
    MD - Erik
    ########

    #Immediate
    # dump a restart file with timestep, reporting time, equillibration time and analysis parameters
    #clean up metadynamics implementation to handle multipel colvars, convergence and more
    #Add metadynamics options to the command line
    #document metadynamics in argparse
    #check if we can run MTD straight from CLI without restart
    #dump a set of restart files labelled MTD after metadynamics, add restart option for these.
    #Make a real plotting function for metadynamics

    #Medium term
    # figure out what automatic sparsity should be for prolif runs
    # fix so that analysis has a self.u
    # makes sure all axes are divided with nice whole number ticks - use seaborne
    # make a setup.py or similair to allow for a pip install (or refactor to compchem cookiecutter)
    # Fix RMSF parallelization bug

    #Long term
    # improve metalloprotein handling https://ash.readthedocs.io/en/latest/Metalloprotein-I.html
    # add the ability to run sequential replicates of the same simulation via argparse

    # add metadynamics
    # add electrostatic field analysis via own code or https://github.com/bunzela/FieldTools
     # implement cyclic equillibration until it converges well, also reimplement equillibration checks 10.1021/acs.jctc.5b00784 https://pymbar.readthedocs.io/en/master/timeseries.html
   


    ######
    Miner - Erik
    ######

    # work on data collection and packaging for single frames as well as whole trajectories
    #link to the MD engine to make a complete workflow


    ######
    Mutation script - Victor
    ######

    Take a PDB file path and mutate it using PLACER --mutate to a new strucutre
    I don´t think we have to do any validation or something here as we can outsource the flagging of
    destabilizing mutations to a optimize module calling LigandMPNN later down the line
    
    ##########
    Docking - Victor
    ##########
    
    Placer + AAScore or something i don´t know

